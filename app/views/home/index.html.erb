<%= link_to 'Logout', destroy_user_session_path %>

<h1>Dashboard</h1>
<div>
  <%= link_to '歩いた記録を追加する', {:controller => 'user_records', :action => 'new'}, :class => 'button icon add' %>
</div>

<div>
<table class="dashboard">
  <thead>
    <tr>
      <td>順位</td>
      <td>人</td>
      <td>現在地</td>
      <td>累計距離</td>
      <td>累計歩数</td>
    </tr>
  </thead>
  <tbody>
  <% @current_statuses.each_with_index do |status, i| %>
  <tr>
    <td><%= i+1 %></td>
    <td><%= image_tag status.user.image %></td>
    <td>
      <%= status.location.number %>: <%= status.location.name %> <br>
      次の <%= status.next_location.name %> まで あと <%= status.next_distance %> Km
    </td>
    <td><%= status.total_distance %> Km </td>
    <td><%= status.total_step %> 歩</td>
  </tr>
  <% end %>
  </tbody>
</table>
</div>



<hr>
<%= image_tag @user.image %>
現在: <%= @user_status.total_step %> 歩、 <%= @user_status.total_distance %> Km <br>
<%= @user_status.location.number %>: <%= @user_status.location.name %> <br>
次の <%= @user_status.next_location.name %> まで あと <%= @user_status.next_distance %> Km <br>
緯度<%= @user_status.lat %>,経度<%= @user_status.lon %> 付近
<br>
<%= link_to 'settings', @user_setting %>
<br>
<%= link_to 'add records', :controller => :user_records, :action => :new %>

<br>
<%= @user.inspect %>

